# ElevenLabs API Key (required for TTS/SFX/Music generation)
# Can also be set via the Settings page in the app
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# LLM API Keys (at least one required for AI auto-assign)
# Can also be set via the Settings page in the app
OPENAI_API_KEY=
MISTRAL_API_KEY=
GEMINI_API_KEY=

# App password (simple auth gate — change this!)
APP_PASSWORD=changeme

# Server port
PORT=3001

# Data directory
DATA_DIR=./data

# Node environment
NODE_ENV=production
